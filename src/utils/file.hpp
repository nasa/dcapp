#pragma once

// dcapp includes

// library includes

// c++ standard includes
#include <string>

std::string dc_utils_filepath_to_canonical(std::string filepath, const std::string &directory);
std::string dc_utils_get_exe_filepath();