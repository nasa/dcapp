#pragma once

// dcapp includes

// library includes

// c++ standard includes

bool doubleEquals(double value1, double value2, double precision = .00001);
bool floatEquals(float value1, float value2, float precision = .00001);
