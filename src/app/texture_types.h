#ifndef DC_APP_TEXTURE_TYPES_H
#define DC_APP_TEXTURE_TYPES_H

#include <stdint.h>

// Texture IDs stay stable for the context lifetime, and zero is invalid.
typedef int DcAppTextureIndex;
typedef uint32_t DcAppTextureId;

#endif
