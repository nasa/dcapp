#include <cstring>
#include <ctime>
#include <sys/time.h>
#include "dcapp.hh"
#include "blinker.hh"

extern "C" void DisplayInit(void)
{
}

extern "C" void DisplayLogic(void)
{
    //*ZOOM += .01;

    //*LATITUDE += 1;
    //*LONGITUDE += 1;

    // printf("%f, %f\n", *LATITUDE, *LONGITUDE);
}
