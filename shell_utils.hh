#include <vector>
#include <string>

extern std::vector<std::string> parseUserPath(void);
extern std::string findExecutablePath(const std::string &);
extern std::string getScriptResult(const std::string &, const std::string &);
